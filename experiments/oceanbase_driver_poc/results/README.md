# 运行结果目录

`run_poc.py` 每次运行都会在此写入一份 `results/<mode>-<runtime>-<timestamp>.json`。
这些文件是结论文档 `docs/oceanbase-driver-poc.md` 的依据：文档里的每条实测结论
都应当能指回其中的某一次运行。

**不要把结果文件提交进仓库。** 其中的连接目标虽已经过脱敏，但仍属于环境信息，
且每次运行都会产生新文件。需要留证时，把对应 JSON 附到需求时间线或留档，
而不是提交到源码树。
